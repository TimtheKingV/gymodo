import SwiftUI

/// Rumpf fuer Aufgabe 8 -- die eigentliche Gestalt kommt dort.
struct UebungsfortschrittView: View {
    let exerciseId: String

    var body: some View {
        Text(exerciseId)
    }
}
